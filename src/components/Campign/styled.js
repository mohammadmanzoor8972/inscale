import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
    root: {
        padding:'10px',
        margin:'0px auto',
        width:"800px",
        marginTop:"50px"

    }
}))
